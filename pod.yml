apiVersion: v1
kind: Pod
metadata:
  name: pythonapi
spec:
  containers:
    - image: docker.io/giochajon/shustr-restapi
      name: pythonapi
      imagePullPolicy: Always
      resources: {}
      ports:
        - containerPort: 5000
